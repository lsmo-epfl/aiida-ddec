---
steps:
- task:           UsePythonVersion@0
  inputs:
    versionSpec:  '$(python.version)'
    architecture: 'x64'

- bash:           |
    pip install pytest-azurepipelines git+https://github.com/kjappelbaum/aiida-cp2k.git@aiida1_multistage
  displayName:    'Install pytest Azure  pipelines'

- script:  python -m pip install -e .[pre-commit]
  displayName: 'Install package with dependencies'
